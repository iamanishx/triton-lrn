[project]
name = "triton-lrn"
version = "0.1.0"
description = "Learning Triton kernels on Windows with CUDA PyTorch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "triton-windows<3.7",
]
